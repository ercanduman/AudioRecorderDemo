<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/HomeFragment">

    <fragment
        android:id="@+id/HomeFragment"
        android:name="ercanduman.android.audiorecorder.ui.main.recording.home.HomeFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_HomeFragment_to_RecordingsFragment"
            app:destination="@id/RecordingsFragment" />
    </fragment>
    <fragment
        android:id="@+id/RecordingsFragment"
        android:name="ercanduman.android.audiorecorder.ui.main.recording.list.RecordingsFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_recordings" />
</navigation>